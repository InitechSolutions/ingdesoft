<h2 style="color:#FF8000;">Olvido su contraseña?</h2>
<h5>Se enviaran la instrucciones para modificar la contraseña a su email.</h5>

<div class="container" style="margin-top:20px;">
<div class="col-lg-8 col-lg-offset-2 col-md-10 col-md-offset-1">
<div class="well">
<div class="media">

<%= form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :post }) do |f| %>
  <%= devise_error_messages! %>

  <div class="field">
    <%= f.label :Email %><br />
    <%= f.email_field :email, autofocus: true,  size: "40x10", placeholder: "ejemplo@ejemplo.com" %>
  </div>

  <div class="actions">
    <%= f.submit "Enviar instrucciones", class:"btn-lg btn-warning" %>
  </div>
<% end %>
</div></div></div></div>
<%= render "users/shared/links" %>
